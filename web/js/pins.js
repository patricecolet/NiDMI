/**
 * @file pins.js
 * @brief Fichier de compatibilité (maintenu pour le build system)
 * 
 * Ce fichier est maintenu pour compatibilité avec le build system.
 * Les fonctions ont été déplacées dans des modules séparés :
 * - ui-utils.js : setOptions()
 * - pin-utils.js : pType(), getRoleDisplayLabel(), replaceTemplate(), getComponentStatusText(), stat()
 * - pin-visual.js : updateBusVisuals(), drawBoard()
 * - pin-list.js : updatePinsList()
 */

/* Ce fichier est maintenu pour compatibilité avec le build system */
