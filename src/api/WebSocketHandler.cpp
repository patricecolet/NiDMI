#include "APICommon.h"

// NOTE: Pour les commits a1a2b30 et antérieurs, onWsEvent est définie dans WebAPI.cpp
// Cette version dans api/WebSocketHandler.cpp est désactivée pour éviter les définitions multiples.
// Le code est commenté pour éviter les erreurs de linkage (définitions multiples).
